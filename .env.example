# Build-time API keys. Set the same names in Vercel's Environment Variables.
# Vite inlines VITE_*-prefixed vars into the client bundle, so these end up
# visible to anyone who reads the deployed JS — restrict them in each
# provider's dashboard before sharing the deploy.

VITE_DEEPGRAM_KEY=
VITE_GEMINI_KEY=
